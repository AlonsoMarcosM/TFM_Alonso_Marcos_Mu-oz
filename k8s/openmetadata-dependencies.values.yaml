# Overrides para entorno local (kind / Docker Desktop Kubernetes)
# Objetivo: stack mínimo y estable para la plataforma (sin Airflow interno).

airflow:
  enabled: false

opensearch:
  # En local de 1 nodo no necesitamos replicas multiples.
  replicas: 1
